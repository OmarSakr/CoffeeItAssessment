<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codevalley.coffeeitassessment">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.AppController"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".main.activities.overview.view.OverviewActivity"
            android:exported="false" />
        <activity
            android:name=".main.activities.coffeExtras.view.CoffeeExtrasActivity"
            android:exported="false" />
        <activity
            android:name=".main.activities.coffeeSize.view.CoffeeSizeActivity"
            android:exported="false" />
        <activity
            android:name=".main.activities.home.view.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".splash.view.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>